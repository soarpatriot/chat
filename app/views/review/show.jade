extends ../layouts/main-layout

block append head

block content
    body
        div(id='review-container',class='container')
            div(class='row-fluid')

                div(id='post-content',class='span8')
                    div(class='row-fluid')
                        div(class='span8')
                            h4 #{post.title}
                    hr
                    div(class='row-fluid')
                        div(class='span8') #{post.content}
                form(id='review-form',action='/review',method='post')
                    input(id='post-id',type='hidden',name='postId',value='#{post._id}')
                    input(id='passed',type='hidden',name='passed',value='')
                div(class='span4 well-no-back')
                    h3 我觉得这篇文章
                    p 有下列情景
                        ul
                            li 政治
                            li 低俗
                            li 广告
                            li 坟
                            li 无意义
                    p
                        button(id='veto', class='btn btn-danger btn-large',name='veto',value='不能发表') 不能发表
                    p 有如下情况
                        ul
                            li 内容真实
                            li 有积极意义
                            li 需要援助
                    p
                        button(id='support', class='btn btn-success btn-large',name='support',value='可以发表') 可以发表
                <!-- Templates -->
                script(type='text/template',id='post-template')
                    div(class='row-fluid')
                        div(class='span8') <%- title %>
                    div(class='row-fluid')
                        div(class='span8') <%- content %>

        script(data-main='/build/app/review',src='/javascripts/require.js')